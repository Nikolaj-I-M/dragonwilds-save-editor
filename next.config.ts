import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Site totalmente estático: nenhum backend é necessário,
  // todo o processamento do save acontece no navegador.
};

export default nextConfig;
